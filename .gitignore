# Sandboxed Trading System - Git Ignore Configuration
# NFR6: Prevent secrets from being committed

# Environment files (contains secrets)
.env
.env.local
.env.*.local
configs/**/.env
configs/**/.env.local

# Docker volumes and data
data/
*.db
*.sqlite

# Logs
*.log
logs/

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE files
.vscode/
.idea/
*.sublime-*

# Build artifacts
bin/
build/
dist/
*.exe

# Go specific
vendor/
*.test
*.out
coverage.html
coverage.txt

# Python specific (for future Epic 2)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
venv/
env/

# Temporary files
tmp/
temp/
*.tmp

# Benchmark results
benchmark_results/
*.benchmark

# Compliance report artifacts (Epic 10 / D9 — story 10.15).
# ``trading-engine compliance-report`` writes a dated JSON per run to
# ``services/trading-engine/compliance-report-<account>-<YYYY-MM-DD>.json``;
# they are reproducible from the audit log + Redis state so we keep
# them out of git.
compliance-report-*.json
services/trading-engine/compliance-report-*.json

# Docker
docker-compose.override.yml

.bmad/
.gemini/
# .claude/ — track shared config (rules, agents, skills, commands), ignore local settings
.claude/settings.local.json
.specify/
specs/
.vscode/